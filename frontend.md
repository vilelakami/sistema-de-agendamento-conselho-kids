Criando pastas:

- pages → telas completas (login, cadastro, dashboard)
- components → pedaços reutilizáveis (botões, inputs, cards)
- services → comunicação do backend (php)
- styles → css
- assets → imagens e ícones